const PageTitle = ({ title, subTitle }) => {
  return (
    <div className="my-7 pt-24 border-t">
      <h1 className="text-7xl font-bold text-left">{title}</h1>
    </div>
  );
};

export default PageTitle;
